#ifndef __RUN_LENGTH_ENCODING_H_
#define __RUN_LENGTH_ENCODING_H_

#include <stdlib.h>
#include <string.h>
#include <stdio.h>
#include <ctype.h>

char* encode(const char*);
char* decode(const char*);

#endif // __RUN_LENGTH_ENCODING_H_
